def execute():
    """v3 LIGHT PREMIUM redesign (owner brief: light colors, premium feel,
    animations, single Book-now hero CTA). Ivory/white/charcoal/gold-bronze,
    Cormorant serif headings, scroll-reveal + hover animations."""
    from blarberine.website_build.loader import rebuild
    rebuild()
